package com.feduss.telegramwear.login.authType.url

import android.app.Application
import androidx.lifecycle.AndroidViewModel

class LoginUrlViewModel(application: Application) : AndroidViewModel(application) {
}